handle correctly multiple connection at libwebsocket level
serial send more than 1char at a time (or at least only for serial)
(re)check leaks
serial: try 3 times to get auth instead once ?
make logo/turtle example ?
tetris: check the end of greying, strangeness happens
recheck "?server=" parameter in url with firefox
(intermittent?) délai bizarre à chaque nouvelle connexion : quand on clique sur les boutons ça ne "part" que plus tard
text: losefocus -> envoi du texte
text option autoupdate -> envoi du texte
download fichier depuis le browser ?? (websocket binary blobs / string+base64 ??)
