handle correctly multiple connection at libwebsocket level
serial send more than 1char at a time (or at least only for serial)
(re)check leaks
wsserver.c: dynamic size for output_buffer_lws
serial: try 3 times to get auth instead once ?
make logo/turtle example ?
tetris: check the end of greying, strangeness happens
